<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/nav_home">

    <fragment
        android:id="@+id/nav_home"
        android:name="ru.er_log.stock.android.ui.exchange.active_lots.ActiveLotsFragment"
        android:label="@string/menu_active_lots"
        tools:layout="@layout/fragment_active_lots" />

    <fragment
        android:id="@+id/nav_gallery"
        android:name="ru.er_log.stock.android.ui.exchange.deals.CompletedDealsFragment"
        android:label="@string/menu_completed_deals"
        tools:layout="@layout/fragment_completed_deals" />
</navigation>