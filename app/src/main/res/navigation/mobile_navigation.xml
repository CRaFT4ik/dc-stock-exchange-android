<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/nav_active_lots">

    <fragment
        android:id="@+id/nav_active_lots"
        android:name="ru.er_log.stock.android.presentation.exchange.active_lots.ActiveLotsFragment"
        android:label="@string/menu_active_lots"
        tools:layout="@layout/fragment_active_lots" />

    <fragment
        android:id="@+id/nav_completed_deals"
        android:name="ru.er_log.stock.android.presentation.exchange.deals.CompletedDealsFragment"
        android:label="@string/menu_completed_deals"
        tools:layout="@layout/fragment_completed_deals" />
</navigation>