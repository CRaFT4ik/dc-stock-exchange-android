<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/app_global_navigation"
    app:startDestination="@id/auth_login">

    <action
        android:id="@+id/action_to_nav_auth_login"
        app:destination="@id/auth_login" />

    <fragment
        android:id="@+id/auth_login"
        android:name="ru.er_log.stock.android.features.auth.login.LoginScreen"
        android:label="@string/nav_title_auth">
        <action
            android:id="@+id/action_auth_login_to_active_lots"
            app:destination="@id/order_book"
            app:popUpTo="@id/auth_login"
            app:popUpToInclusive="true" />
    </fragment>

    <fragment
        android:id="@+id/order_book"
        android:name="ru.er_log.stock.android.features.home.exchange.order_book.OrderBookFragment"
        android:label="@string/nav_title_order_book"
        tools:layout="@layout/fragment_order_book" />

    <fragment
        android:id="@+id/completed_deals"
        android:name="ru.er_log.stock.android.features.home.exchange.deals.CompletedDealsFragment"
        android:label="@string/menu_completed_deals"
        tools:layout="@layout/fragment_completed_deals" />
</navigation>