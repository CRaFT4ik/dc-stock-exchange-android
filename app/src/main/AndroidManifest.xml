<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.er_log.stock.android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application android:name=".base.StockExchangeApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Dcstockexchangeandroid">
        <activity
            android:name="ru.er_log.stock.android.features.exchange.ExchangeActivity"
            android:exported="false"
            android:label="@string/title_activity_account"
            android:theme="@style/Theme.Dcstockexchangeandroid.NoActionBar" />
        <activity
            android:name="ru.er_log.stock.android.features.auth.AuthActivity"
            android:label="@string/title_activity_auth"
            android:exported="false" />
        <activity
            android:name=".features.MainActivity"
            android:exported="true"
            android:noHistory="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>